name: Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
permissions:
  contents: write  # 允许创建 release 和上传附件
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: ''
          - platform: 'macos-latest'
            args: '--target aarch64-apple-darwin'
          - platform: 'ubuntu-22.04'
            args: ''
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10.29.3  # 或者你本地使用的版本

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.19.0'
          cache: 'pnpm'  # 改为 pnpm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install dependencies (Ubuntu)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install dependencies
        run: pnpm install --frozen-lockfile  # 使用 pnpm

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Teresa AI v__VERSION__'
          releaseBody: |
            ## Teresa AI v__VERSION__

            ### 新功能
            - 📷 **视觉功能**：支持截图或上传图片发送给 AI，AI 根据图片内容进行分析和回复（需模型支持视觉输入，如 gpt-4o、claude-3 等）
            - 🖼️ 聊天窗口内置截图按钮与图片上传按钮，发送前可预览并移除待发图片

            ### 下载说明
            - **Windows**：下载 `.msi` 或 `.exe` 安装包
            - **macOS (Intel)**：下载 `.dmg`（无 `aarch64` 后缀）
            - **macOS (Apple Silicon)**：下载 `aarch64` 版 `.dmg`
            - **Linux**：下载 `.deb` 或 `.AppImage`

            See the assets to download this version and install.
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
